spring:
  datasource:
    username: root
    password: "0924"
    url: jdbc:mariadb://localhost:3306/locarie_test

  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com
    port: 587
    username: luty@locarie.com
    password: "gbqm ukpt dbsr uqzq"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  issuer: locarie
  secret: lulu-locarie
  expiration: 259200  # unit: s, 30 days

validation:
  code:
    expire: 300  # unit: s

logging:
  level:
    org.hibernate.sql: debug


